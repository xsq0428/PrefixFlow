# 移动端卡片式布局更新说明

## 更新内容

### 1. 修复编辑按钮点击问题

**问题**: 之前使用不存在的 `editProduct()` 和 `editPrefix()` 函数

**修复**: 改为直接调用 `openProductModal(id)` 和 `openPrefixModal(id)`

```javascript
// 之前（错误）
<button onclick="editProduct(1)">编辑</button>

// 现在（正确）
<button onclick="openProductModal(1)">编辑</button>
```

### 2. 添加卡片式布局

#### 桌面端（>768px）
- 显示表格视图
- 隐藏卡片视图

#### 移动端（≤768px）
- 隐藏表格视图
- 显示卡片视图

### 3. 卡片设计

#### 产品管理卡片
```
┌─────────────────────────────┐
│ 产品名称           [启用]   │ ← 卡片头部（标题 + 状态）
├─────────────────────────────┤
│ ID:         1               │
│ 描述：      产品描述        │ ← 卡片主体（详细信息）
│ 下载链接：  点击下载        │
│ 关联规则：  5 个             │
├─────────────────────────────┤
│   [编辑]      [删除]        │ ← 卡片底部（操作按钮）
└─────────────────────────────┘
```

#### 前缀规则卡片
```
┌─────────────────────────────┐
│ ABC              优先级 10  │ ← 前缀 + 优先级标签
├─────────────────────────────┤
│ ID:         1               │
│ 关联产品：  产品名称        │
│ 创建时间：  2026-06-07      │
├─────────────────────────────┤
│   [编辑]      [删除]        │
└─────────────────────────────┘
```

## 样式细节

### 卡片头部（.card-header）
- 产品名称/前缀（粗体）
- 状态标签（右侧）
- 底部边框分隔

### 卡片主体（.card-body）
- Grid 布局，垂直排列
- 标签 + 值的信息行
- 标签灰色，值黑色
- 链接蓝色可点击

### 卡片底部（.card-footer）
- 边框分隔
- 两个操作按钮：
  - **编辑**: 蓝色背景，白色文字
  - **删除**: 浅红色背景，红色文字（hover 变红底白字）

### 状态标签
- **启用**: 绿色背景 (#d4edda) + 深绿文字
- **禁用**: 红色背景 (#f8d7da) + 深红文字

## 响应式断点

```css
/* 默认：桌面端显示表格 */
.card-list { display: none; }
.table-container { display: block; }

/* 移动端（≤768px） */
@media (max-width: 768px) {
    .card-list { display: grid; gap: 15px; }
    .table-container { display: none; }
}
```

## JavaScript 改动

### loadProducts() 函数
现在同时渲染表格和卡片两种视图：

```javascript
// 渲染表格视图（桌面端）
tbody.innerHTML = data.data.map(product => `...`);

// 渲染卡片视图（移动端）
cardList.innerHTML = data.data.map(product => `
    <div class="card">
        <div class="card-header">...</div>
        <div class="card-body">...</div>
        <div class="card-footer">
            <button onclick="openProductModal(${product.id})">编辑</button>
            <button onclick="deleteProduct(${product.id})">删除</button>
        </div>
    </div>
`).join('');
```

### loadPrefixes() 函数
同样的双视图渲染逻辑。

## 测试方法

### Chrome 开发者工具
1. F12 打开开发者工具
2. Ctrl+Shift+M 切换设备模式
3. 选择设备：
   - iPhone 12 Pro (390x844)
   - iPhone SE (375x667)
   - iPad Air (820x1180)
4. 刷新页面查看卡片布局

### 实际设备
访问：https://80-13f2e295ada3c79e.monkeycode-ai.online/admin/login.php

## 效果对比

### 桌面端布局
```
┌────────────────────────────────────────────┐
│ Logo        [产品管理] [前缀规则]           │
│ [搜索框     ][搜索][+添加产品]            │
│ ┌─────────────────────────────────────┐   │
│ │ 表格（7 列：ID, 名称，描述...）      │   │
│ └─────────────────────────────────────┘   │
└────────────────────────────────────────────┘
```

### 移动端布局
```
┌─────────────────────┐
│       Logo          │
│ [产品管理] [前缀规则]│  ← 可滑动
│ [+添加产品 - 全宽]   │
│ ┌─────────────────┐ │
│ │ 产品名称 [启用]│ │ ← 卡片 1
│ │ ID: 1           │ │
│ │ 描述：xxx       │ │
│ │ [编辑][删除]    │ │
│ ├─────────────────┤ │
│ │ 产品名称 [禁用]│ │ ← 卡片 2
│ │ ID: 2           │ │
│ │ ...             │ │
│ └─────────────────┘ │
└─────────────────────┘
```

## 性能优化

1. **懒加载**: 卡片和表格同时渲染，但只显示一种
2. **Grid 布局**: 使用 CSS Grid 替代 Float
3. **Touch 优化**: `-webkit-overflow-scrolling: touch` 平滑滚动
4. **Transition**: 悬停效果使用 CSS transition

## 兼容性

- ✅ iOS Safari 12+
- ✅ Android Chrome 80+
- ✅ 微信内置浏览器
- ✅ 桌面浏览器（Chrome, Firefox, Safari, Edge）

## 已知问题

暂无

## 后续优化建议

1. 添加下拉刷新功能
2. 添加无限滚动（分页加载）
3. 添加卡片之间的间距动画
4. 添加左滑删除手势
5. 添加批量操作模式
