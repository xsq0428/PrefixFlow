# 宝塔快速部署教程

## 1. 上传项目

1. 登录宝塔面板 → **文件**
2. 进入 `/www/wwwroot/`
3. 上传项目压缩包并解压，重命名为 `license-system`

## 2. 添加网站

1. **网站** → **添加站点**
2. 域名：填你的域名或 IP
3. 根目录：选择 `/www/wwwroot/license-system`
4. PHP 版本：选 **PHP 7.4+**
5. 数据库：选择 MySQL，记录数据库名、用户名、密码

## 3. 初始化系统

1. 浏览器访问：`http://你的域名/install.php`
2. 填写：数据库名、数据库地址（localhost）、端口（3306）、数据库用户名、密码
3. 填写管理员用户名和密码（默认 admin / admin123）
4. 可选是否安装示例数据
5. 点击「开始安装」

安装完成后会自动：创建数据库 → 建表 → 初始化管理员 → 生成 `.env` 文件

## 4. 验证

| 页面 | 地址 |
|------|------|
| 查询首页 | `http://你的域名/` |
| 后台登录 | `http://你的域名/admin/login.php` |

首次登录后请立即修改密码。
